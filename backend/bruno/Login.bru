meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "terravista"
  }
}

docs {
  Mock login. Returns a bearer token for the web/mobile clients.
}
