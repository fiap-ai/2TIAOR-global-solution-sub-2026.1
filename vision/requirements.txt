# TerraVista — Vision module dependencies
numpy>=1.26
pandas>=2.1
pillow>=10.0
matplotlib>=3.8
jupyter>=1.0
# Optional object-detection layer (heavy; the scene analyzer works without it)
ultralytics>=8.1
